type User {
    id: ID!
    name: String!
    email: String
}

type Article {
    id:ID!

}

type Query {
     users: [User!]! @all(model: "App\\Models\\User")
    user(id: ID @eq): User @find(model: "App\\Models\\User")
}
